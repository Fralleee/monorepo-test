/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */

import { type QueryContext, type CrudContract, type PermissionCheckerContext } from "@zenstackhq/runtime";
import { allFieldsEqual } from "@zenstackhq/runtime/validation";
import { type PolicyDef, type PermissionCheckerConstraint } from "@zenstackhq/runtime/enhancements/node";

const policy: PolicyDef = {
    policy: {
        clinic: {
            modelLevel: {
                read: { guard: Clinic_read, },
                create: { guard: Clinic_create, inputChecker: Clinic_create_input, },
                update: { guard: Clinic_update, },
                postUpdate: { guard: Clinic_postUpdate, },
                delete: { guard: Clinic_delete, }
            },
            fieldLevel: {
                read:
                {

                },
                update:
                {

                },
            },

        },
        treatment: {
            modelLevel: {
                read: { guard: Treatment_read, },
                create: { guard: Treatment_create, inputChecker: Treatment_create_input, },
                update: { guard: Treatment_update, },
                postUpdate: { guard: Treatment_postUpdate, },
                delete: { guard: Treatment_delete, }
            },
            fieldLevel: {
                read:
                {

                },
                update:
                {

                },
            },

        },
        treatmentsByClinic: {
            modelLevel: {
                read: { guard: TreatmentsByClinic_read, },
                create: { guard: TreatmentsByClinic_create, inputChecker: TreatmentsByClinic_create_input, },
                update: { guard: TreatmentsByClinic_update, },
                postUpdate: { guard: TreatmentsByClinic_postUpdate, },
                delete: { guard: TreatmentsByClinic_delete, }
            },
            fieldLevel: {
                read:
                {

                },
                update:
                {

                },
            },

        },
    },
    validation: {
        clinic: { hasValidation: false },
        treatment: { hasValidation: false },
        treatmentsByClinic: { hasValidation: false },
    },
    authSelector: { "role": true },
};

function Clinic_read(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if ((user != null)) { return { AND: [] }; } return { OR: [] };
}

function $check_Clinic_read(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if ((user != null)) { return true; }

    return false;
}

function Clinic_create(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return { AND: [] }; } return { OR: [] };
}

function $check_Clinic_create(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return true; }

    return false;
}

function Clinic_create_input(input: any, context: QueryContext): boolean {
    const user: any = context.user ?? null;
    return (user?.role == 'ADMIN')
}

function Clinic_update(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return { AND: [] }; } return { OR: [] };
}

function $check_Clinic_update(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return true; }

    return false;
}

function Clinic_postUpdate(context: QueryContext, db: CrudContract): any {
    return { AND: [] };
}

function $check_Clinic_postUpdate(input: any, context: QueryContext): any {
    return true;
}

function Clinic_delete(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return { AND: [] }; } return { OR: [] };
}

function $check_Clinic_delete(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return true; }

    return false;
}

function Treatment_read(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if ((user != null)) { return { AND: [] }; } return { OR: [] };
}

function $check_Treatment_read(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if ((user != null)) { return true; }

    return false;
}

function Treatment_create(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return { AND: [] }; } return { OR: [] };
}

function $check_Treatment_create(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return true; }

    return false;
}

function Treatment_create_input(input: any, context: QueryContext): boolean {
    const user: any = context.user ?? null;
    return (user?.role == 'ADMIN')
}

function Treatment_update(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return { AND: [] }; } return { OR: [] };
}

function $check_Treatment_update(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return true; }

    return false;
}

function Treatment_postUpdate(context: QueryContext, db: CrudContract): any {
    return { AND: [] };
}

function $check_Treatment_postUpdate(input: any, context: QueryContext): any {
    return true;
}

function Treatment_delete(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return { AND: [] }; } return { OR: [] };
}

function $check_Treatment_delete(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return true; }

    return false;
}

function TreatmentsByClinic_read(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if ((user != null)) { return { AND: [] }; } return { OR: [] };
}

function $check_TreatmentsByClinic_read(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if ((user != null)) { return true; }

    return false;
}

function TreatmentsByClinic_create(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return { AND: [] }; } return { OR: [] };
}

function $check_TreatmentsByClinic_create(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return true; }

    return false;
}

function TreatmentsByClinic_create_input(input: any, context: QueryContext): boolean {
    const user: any = context.user ?? null;
    return (user?.role == 'ADMIN')
}

function TreatmentsByClinic_update(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return { AND: [] }; } return { OR: [] };
}

function $check_TreatmentsByClinic_update(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return true; }

    return false;
}

function TreatmentsByClinic_postUpdate(context: QueryContext, db: CrudContract): any {
    return { AND: [] };
}

function $check_TreatmentsByClinic_postUpdate(input: any, context: QueryContext): any {
    return true;
}

function TreatmentsByClinic_delete(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return { AND: [] }; } return { OR: [] };
}

function $check_TreatmentsByClinic_delete(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if ((user?.role == 'ADMIN')) { return true; }

    return false;
}
export default policy
